#include "main.h"
/**
 * _islower - int _islower
 * Description: Pritn alphebet in lower case
 * Return: void
 * @c: int c
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
